# Wrangler configuration for deploying to Cloudflare Pages
name = "essencetreks"
compatibility_date = "2024-05-17"

[site]
bucket = "dist"
entry-point = "."

[build]
command = "npm run build"

[env.production]
# Add production-specific settings here if needed
